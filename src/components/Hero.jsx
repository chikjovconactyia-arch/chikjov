import React from 'react';
import Slideshow from './ui/Slideshow';
import './Hero.css';

const Hero = () => {
    return (
        <section id="seu-aplicativo" className="hero-section w-full bg-white">
            <Slideshow />
        </section>
    );
};

export default Hero;
